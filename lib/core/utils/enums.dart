enum BenchmarkStatus {
  initial,
  loading,
  loaded,
  running,
  completed,
  error,
}

enum ViewMode { list, grid }

enum ScenarioType {
  cpuProcessingPipeline,
  memoryStateHistory,
  uiGranularUpdates,
}
